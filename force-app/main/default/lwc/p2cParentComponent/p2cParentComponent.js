import { LightningElement } from 'lwc';

export default class P2cParentComponent extends LightningElement {
    message = "Message from the parent";
    cardHeading = "Parent to Child Communication !";
}